# Prog-2-CW

# To run requires user to download flask & supabase through python package manager; Personally also recommend usinga virtual enviroment to run as to avoid version clash although not required.